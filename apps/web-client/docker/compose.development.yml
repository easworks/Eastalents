name: eas-development

services:
  web-client:
    container_name: web-client-development
    extends:
      file: compose.extend.yml
      service: web-client
    networks:
      - eas-development

networks:
  eas-development:
    name: eas-development
