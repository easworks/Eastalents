name: eas-development

services:
  accounts-client:
    container_name: accounts-client-development
    extends:
      file: compose.extend.yml
      service: accounts-client
    networks:
      - eas-development

networks:
  eas-development:
    name: eas-development
