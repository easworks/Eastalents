services:
  api:
    container_name: api
    build: .
    ports:
      - ${PORT:?}:4200
    environment:
      - NODE_ENV=${NODE_ENV:?}
      - PORT=4200
      - MONGODB
      - JWT_PUBLIC_KEY
      - JWT_PRIVATE_KEY
      - JWT_ISSUER
      - AUTH_HOST
      - AUTH_HOST_OAUTH_HANDLER
